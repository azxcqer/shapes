///reset the relative vars so the creator works properly. also an init
//the width used to set the rows
targetWidth = sprite_get_width(targetSprite)
//the height used to set the lines
targetHeight = sprite_get_height(targetSprite)
